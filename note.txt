7/08/25 :

order_table change

delete columns:customer_name,customer_email,customer_phone,customer_address
new added columns:billing_information, billing_address_id


[8/10/2025]
ALTER TABLE `notifications` ADD `seller_id` BIGINT UNSIGNED NULL AFTER `user_id`;
ALTER TABLE `notifications` CHANGE `user_id` `user_id` BIGINT UNSIGNED NULL;

[8/11/2025]
ALTER TABLE `billing_addresses` ADD `is_default` TINYINT(1) NOT NULL DEFAULT '0' AFTER `district_id`;
update  `billing_addresses` set `type` = 1
ALTER TABLE `billing_addresses` CHANGE `type` `type` TINYINT(1) NULL DEFAULT NULL;

[8/14/2025]
ALTER TABLE `reviews` ADD `order_id` BIGINT UNSIGNED NOT NULL AFTER `id`, ADD `order_item_id` BIGINT UNSIGNED NOT NULL AFTER `order_id`;
ALTER TABLE `order_items` ADD `is_reviewed` TINYINT(1) NOT NULL DEFAULT '0' AFTER `sub_total`;

[8/24/2025]

delete columns form users and sellers country_id, state_id, zip


[09/23/2025]

button reload
details page new tab 
product filter

[10/22/2025]

seller registration password toggle not working
 admin panel dashboard fontawesome missing
 large image not upload user registration
 quantity > stock visual problem
 seller select toggle problem
 order details not dynamic fully
 stock check when quantity update on cart details
 selling price show on cart details